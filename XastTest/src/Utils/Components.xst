module Utils.Components exports { Pos, Velocity, Player, Box, Bool, True, False, Shape, Circle, Rectangle }

type Pos = Pos {
   x : Int,
   y : Int
};

type Velocity = Velocity Int Int;

type Player = Player;

type Box a = Box a;

type Bool = True | False;

type Shape
   = Circle Int
   | Rectangle Int Int;
